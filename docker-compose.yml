services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: cmp
      POSTGRES_PASSWORD: cmp
      POSTGRES_DB: cmp
    ports:
      - '5432:5432'
    volumes:
      - cmp_postgres_data:/var/lib/postgresql/data

volumes:
  cmp_postgres_data:
