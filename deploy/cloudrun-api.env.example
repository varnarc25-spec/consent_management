# One-time setup: create secrets for Cloud Run API service.
# Run from project root after: gcloud config set project myweb-503314

# 1. Database (Neon PostgreSQL)
# echo -n "postgresql://USER:PASSWORD@HOST/DB?sslmode=require" | \
#   gcloud secrets create DATABASE_URL --data-file=-

# 2. JWT secrets (generate with: openssl rand -hex 32)
# echo -n "your-access-secret" | gcloud secrets create JWT_ACCESS_SECRET --data-file=-
# echo -n "your-refresh-secret" | gcloud secrets create JWT_REFRESH_SECRET --data-file=-

# 3. Grant Cloud Run access to secrets
# PROJECT_NUMBER=$(gcloud projects describe myweb-503314 --format='value(projectNumber)')
# RUN_SA="${PROJECT_NUMBER}-compute@developer.gserviceaccount.com"
# for SECRET in DATABASE_URL JWT_ACCESS_SECRET JWT_REFRESH_SECRET; do
#   gcloud secrets add-iam-policy-binding $SECRET \
#     --member="serviceAccount:$RUN_SA" \
#     --role="roles/secretmanager.secretAccessor"
# done

# 4. Set env vars on the running service (quick fix without redeploying build):
# gcloud run services update consent-management-api \
#   --region us-central1 \
#   --port 8080 \
#   --set-env-vars="NODE_ENV=production,API_PREFIX=api/v1,ADMIN_URL=https://YOUR-ADMIN-URL,API_URL=https://YOUR-API-URL,EMAIL_VERIFICATION_ENABLED=false,DOMAIN_AUTO_VERIFY=false" \
#   --set-secrets="DATABASE_URL=DATABASE_URL:latest,JWT_ACCESS_SECRET=JWT_ACCESS_SECRET:latest,JWT_REFRESH_SECRET=JWT_REFRESH_SECRET:latest"
