# Cloud Build for consent-management-admin (Cloud Run GitHub deploy trigger).
# In GCP Console: Configuration → Cloud Build configuration file → Repository → cloudbuild.admin.yaml
#
# Set substitution variables on the trigger (or defaults below):
#   _NEXT_PUBLIC_API_URL  e.g. https://api.yourdomain.com/api/v1
#   _AUTH0_DOMAIN         e.g. your-tenant.us.auth0.com

steps:
  - name: gcr.io/cloud-builders/docker
    id: Build
    args:
      - build
      - '--no-cache'
      - '-t'
      - '$_AR_HOSTNAME/$_AR_PROJECT_ID/$_AR_REPOSITORY/$REPO_NAME/$_SERVICE_NAME:$COMMIT_SHA'
      - '-f'
      - docker/Dockerfile.admin
      - '--build-arg'
      - NEXT_PUBLIC_API_URL=${_NEXT_PUBLIC_API_URL}
      - '--build-arg'
      - AUTH0_DOMAIN=${_AUTH0_DOMAIN}
      - .

  - name: gcr.io/cloud-builders/docker
    id: Push
    args:
      - push
      - '$_AR_HOSTNAME/$_AR_PROJECT_ID/$_AR_REPOSITORY/$REPO_NAME/$_SERVICE_NAME:$COMMIT_SHA'

  - name: gcr.io/google.com/cloudsdktool/cloud-sdk:slim
    id: Deploy
    entrypoint: gcloud
    args:
      - run
      - services
      - update
      - $_SERVICE_NAME
      - '--platform=managed'
      - '--image=$_AR_HOSTNAME/$_AR_PROJECT_ID/$_AR_REPOSITORY/$REPO_NAME/$_SERVICE_NAME:$COMMIT_SHA'
      - '--region=$_DEPLOY_REGION'
      - '--port=8080'
      - >-
        --set-env-vars=NODE_ENV=production,APP_BASE_URL=${_ADMIN_URL},AUTH0_DOMAIN=${_AUTH0_DOMAIN},AUTH0_CLIENT_ID=${_AUTH0_CLIENT_ID},NEXT_PUBLIC_AUTH0_DOMAIN=${_AUTH0_DOMAIN}
      - '--set-secrets=CM_AUTH0_CLIENT_ID=CM_AUTH0_CLIENT_ID:latest,CM_AUTH0_CLIENT_SECRET=CM_AUTH0_CLIENT_SECRET:latest,AUTH0_SECRET=AUTH0_SECRET:latest'
      - '--labels=managed-by=gcp-cloud-build-deploy-cloud-run,commit-sha=$COMMIT_SHA,gcb-build-id=$BUILD_ID,gcb-trigger-id=$_TRIGGER_ID'
      - '--quiet'

images:
  - '$_AR_HOSTNAME/$_AR_PROJECT_ID/$_AR_REPOSITORY/$REPO_NAME/$_SERVICE_NAME:$COMMIT_SHA'

options:
  substitutionOption: ALLOW_LOOSE
  logging: CLOUD_LOGGING_ONLY

substitutions:
  _NEXT_PUBLIC_API_URL: https://consent-management-api-414895350436.us-central1.run.app/api/v1
  _ADMIN_URL: https://consent-management-admin-414895350436.us-central1.run.app
  _AUTH0_DOMAIN: your-tenant.us.auth0.com
  _AUTH0_CLIENT_ID: your-auth0-client-id
