# Cloud Build for consent-management-api (Cloud Run GitHub deploy trigger).
# In GCP Console: Configuration → Cloud Build configuration file → Repository → cloudbuild.api.yaml
#
# Before first deploy, create secrets in Secret Manager:
#   DATABASE_URL, JWT_ACCESS_SECRET, JWT_REFRESH_SECRET
# Then set substitution variables on the trigger (see substitutions below).

steps:
  - name: gcr.io/cloud-builders/docker
    id: Build
    args:
      - build
      - '--no-cache'
      - '-t'
      - '$_AR_HOSTNAME/$_AR_PROJECT_ID/$_AR_REPOSITORY/$REPO_NAME/$_SERVICE_NAME:$COMMIT_SHA'
      - '-f'
      - docker/Dockerfile.api
      - .

  - name: gcr.io/cloud-builders/docker
    id: Push
    args:
      - push
      - '$_AR_HOSTNAME/$_AR_PROJECT_ID/$_AR_REPOSITORY/$REPO_NAME/$_SERVICE_NAME:$COMMIT_SHA'

  - name: gcr.io/google.com/cloudsdktool/cloud-sdk:slim
    id: Deploy
    entrypoint: gcloud
    args:
      - run
      - services
      - update
      - $_SERVICE_NAME
      - '--platform=managed'
      - '--image=$_AR_HOSTNAME/$_AR_PROJECT_ID/$_AR_REPOSITORY/$REPO_NAME/$_SERVICE_NAME:$COMMIT_SHA'
      - '--region=$_DEPLOY_REGION'
      - '--port=8080'
      - '--startup-probe=initialDelaySeconds=15,timeoutSeconds=5,periodSeconds=10,failureThreshold=12,httpGet.path=/api/v1/health,httpGet.port=8080'
      - >-
        --set-env-vars=NODE_ENV=production,API_PREFIX=api/v1,API_URL=${_API_URL},ADMIN_URL=${_ADMIN_URL},WEB_URL=${_WEB_URL},EMAIL_VERIFICATION_ENABLED=false,DOMAIN_AUTO_VERIFY=false,AUTH0_DOMAIN=${_AUTH0_DOMAIN},AUTH0_AUDIENCE=${_AUTH0_AUDIENCE},AUTH0_ISSUER_URL=https://${_AUTH0_DOMAIN}/
      - '--set-secrets=DATABASE_URL=DATABASE_URL:latest,JWT_ACCESS_SECRET=JWT_ACCESS_SECRET:latest,JWT_REFRESH_SECRET=JWT_REFRESH_SECRET:latest'
      - '--labels=managed-by=gcp-cloud-build-deploy-cloud-run,commit-sha=$COMMIT_SHA,gcb-build-id=$BUILD_ID,gcb-trigger-id=$_TRIGGER_ID'
      - '--quiet'

images:
  - '$_AR_HOSTNAME/$_AR_PROJECT_ID/$_AR_REPOSITORY/$REPO_NAME/$_SERVICE_NAME:$COMMIT_SHA'

options:
  substitutionOption: ALLOW_LOOSE
  logging: CLOUD_LOGGING_ONLY

substitutions:
  _API_URL: https://consent-management-api-414895350436.us-central1.run.app/api/v1
  _ADMIN_URL: https://consent-management-admin-414895350436.us-central1.run.app
  _WEB_URL: https://consent-management-web-414895350436.us-central1.run.app
  _AUTH0_DOMAIN: dev-varnarc.us.auth0.com
  _AUTH0_AUDIENCE: https://api.consent-management.varnarc.com
